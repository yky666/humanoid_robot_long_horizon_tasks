retargeting:
  type: vector
  urdf_path: rh56dftp_hand/rh56dftp_left.urdf

  # Target refers to the retargeting target, which is the robot hand
  target_joint_names: [ 'left_little_1_joint', 'left_ring_1_joint', 'left_middle_1_joint', 'left_index_1_joint',
                        'left_thumb_1_joint', 'left_thumb_swing_joint' ]
  target_origin_link_names: [ "base", "base", "base", "base", "base" ]
  target_task_link_names: [ "thumb_tip",  "index_tip", "middle_tip", "ring_tip", "pinky_tip" ]
  scaling_factor: 1.15

  # Source refers to the retargeting input, which usually corresponds to the human hand
  # The joint indices of human hand joint which corresponds to each link in the target_link_names
  target_link_human_indices: [ [ 0, 0, 0, 0, 0 ], [ 4, 8, 12, 16, 20 ] ]

  # A smaller alpha means stronger filtering, i.e. more smooth but also larger latency
  low_pass_alpha: 1.0
